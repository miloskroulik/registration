# Schema for the configuration files of the Registration Confirmation module.

registration.type.*.third_party.registration_confirmation:
  type: mapping
  label: 'Confirmation email settings'
  mapping:
    registrant_email:
      type: mapping
      label: 'Registrant confirmation mail settings'
      mapping:
        registrant_email_enable:
          label: 'Enable registrant confirmation email'
          type: boolean
        registrant_email_subject:
          label: 'registrant Confirmation email subject'
          type: label
        registrant_email_message:
          label: 'Registrant confirmation email message'
          type: text_format
    admin_email:
      type: mapping
      label: 'Admin confirmation mail settings'
      mapping:
        admin_email_enable:
          label: 'Enable admin confirmation email'
          type: boolean
        admin_email_subject:
          label: 'Admin confirmation email subject'
          type: label
        admin_email_message:
          label: 'Admin confirmation email message'
          type: text_format
        send_registrant_confirmation_copy:
          label: 'Send copy of registrant confirmation message to the admin'
          type: boolean
